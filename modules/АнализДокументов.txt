&НаСервере
Функция ЗагрузитьДокументНаСервере(АдресФайлаВоВременномХранилище, ИмяФайла)

	Возврат ЗагрузитьДокументИзХранилища(
		АдресФайлаВоВременномХранилище,
		ИмяФайла
	);

КонецФункции


&НаКлиенте
Процедура ЗагрузитьДокументНажатие(Команда)

	Если ПустаяСтрока(АдресФайлаВоВременномХранилище) Тогда
		Сообщить("Сначала выберите файл.");
		Возврат;
	КонецЕсли;

	Попытка

		Результат = ЗагрузитьДокументНаСервере(
			АдресФайлаВоВременномХранилище,
			ИмяФайла
		);

		ОтветСервера = Результат;

		Данные = ПрочитатьJSONИзСтроки(Результат);

		Если Данные.Свойство("id") Тогда
			ИДДокумента = Данные.id;
		КонецЕсли;

		Если Данные.Свойство("document_type") Тогда
			ТипДокумента = Данные.document_type;
			ТипДокументаПредставление = ПредставлениеТипаДокумента(ТипДокумента);
		КонецЕсли;

		Если Данные.Свойство("status") Тогда
			СтатусДокумента = Данные.status;
			СтатусДокументаПредставление = ПредставлениеСтатусаДокумента(СтатусДокумента);
		КонецЕсли;

		Если Данные.Свойство("ocr_used") Тогда
			OCRИспользовался = Данные.ocr_used;
		КонецЕсли;

		Сообщить("Документ загружен. ID: " + Строка(ИДДокумента));

	Исключение
		Сообщить("Ошибка загрузки документа: " + ОписаниеОшибки());
	КонецПопытки;

КонецПроцедуры


&НаКлиенте
Процедура ВыбратьФайлНажатие(Команда)

	Оповещение = Новый ОписаниеОповещения("ПослеВыбораФайла", ЭтаФорма);

	НачатьПомещениеФайла(
		Оповещение,
		,
		,
		Истина,
		УникальныйИдентификатор
	);

КонецПроцедуры 


&НаКлиенте
Процедура ПослеВыбораФайла(Результат, Адрес, ВыбранноеИмяФайла, ДополнительныеПараметры) Экспорт

	Если Не Результат Тогда
		Сообщить("Файл не выбран.");
		Возврат;
	КонецЕсли;

	АдресФайлаВоВременномХранилище = Адрес;
	ИмяФайла = ВыбранноеИмяФайла;
	ПутьКФайлу = ВыбранноеИмяФайла;

	Сообщить("Файл выбран: " + ИмяФайла);

КонецПроцедуры 

&НаСервере
Функция ВыполнитьOCRНаСервере(ИДДокумента, ForceOCR)

	Возврат ВыполнитьOCRДокумента(ИДДокумента, ForceOCR);

КонецФункции


&НаСервере
Функция ПолучитьOCRРезультатНаСервере(ИДДокумента)

	Возврат ПолучитьOCRРезультатДокумента(ИДДокумента);

КонецФункции

&НаКлиенте
Процедура ЗаполнитьПоляOCRИзОтвета(Данные)

	Если Данные.Свойство("status") Тогда
		СтатусOCR = Данные.status;
		СтатусOCRПредставление = ПредставлениеСтатусаOCR(СтатусOCR);
	КонецЕсли;

	Если Данные.Свойство("document_type") Тогда
		ТипДокумента = Данные.document_type;
		ТипДокументаПредставление = ПредставлениеТипаДокумента(ТипДокумента);
	КонецЕсли;

	Если Данные.Свойство("ocr_from_cache") Тогда
		OCRИзКэша = Данные.ocr_from_cache;

		Если OCRИзКэша Тогда
			OCRИсточник = "Сохранённый результат";
		Иначе
			OCRИсточник = "Новый результат OCR";
		КонецЕсли;
	КонецЕсли;

	Если Данные.Свойство("ocr_result_dir") Тогда
		OCRКаталогРезультата = Данные.ocr_result_dir;
	КонецЕсли;

	Если Данные.Свойство("text_length") Тогда
		ДлинаOCRТекста = Строка(Данные.text_length) + " символов";
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура РаспознатьДокументНажатие(Команда)

	Если ИДДокумента = 0 Тогда
		Сообщить("Сначала загрузите документ.");
		Возврат;
	КонецЕсли;

	Попытка

		СтатусOCR = "processing";
		СтатусOCRПредставление = "OCR запускается...";

		OCRПрогресс = 0;
		OCRПрогрессТекст = "Запуск OCR...";

		Результат = ЗапуститьOCRНаСервере(ИДДокумента, Ложь);

		ОтветСервера = Результат;

		Данные = ПрочитатьJSONИзСтроки(Результат);

		Если Данные.Свойство("task_id") Тогда

			OCRTaskID = Данные.task_id;

			OCRПрогресс = 5;
			OCRПрогрессТекст = "OCR запущен... 5%";

		Иначе

			OCRПрогресс = 0;
			OCRПрогрессТекст = "Не удалось получить идентификатор задачи";

			Сообщить("OCR запущен, но сервер не вернул task_id.");
			Возврат;

		КонецЕсли;

		ПодключитьОбработчикОжидания("ПроверитьСтатусOCR", 5, Ложь);

		Сообщить("OCR запущен.");

	Исключение

		СтатусOCR = "failed";
		СтатусOCRПредставление = "Ошибка запуска OCR";

		OCRПрогресс = 0;
		OCRПрогрессТекст = "Ошибка запуска OCR";

		Сообщить("Ошибка запуска OCR: " + ОписаниеОшибки());

	КонецПопытки;

КонецПроцедуры

&НаКлиенте
Процедура ПроверитьСтатусOCR()

	Если ПустаяСтрока(OCRTaskID) Тогда
		ОтключитьОбработчикОжидания("ПроверитьСтатусOCR");
		Возврат;
	КонецЕсли;

	Попытка

		Результат = ПолучитьСтатусЗадачиНаСервере(OCRTaskID);
		Данные = ПрочитатьJSONИзСтроки(Результат);

		Если Не Данные.Свойство("status") Тогда
			Возврат;
		КонецЕсли;

		Если Данные.status = "pending" Тогда

			СтатусOCR = "pending";
			СтатусOCRПредставление = "Ожидание запуска извлечения текста...";

			Если OCRПрогресс < 15 Тогда
				OCRПрогресс = OCRПрогресс + 2;
			КонецЕсли;

			OCRПрогрессТекст = "Ожидание запуска извлечения текста... "
				+ Формат(OCRПрогресс, "ЧГ=0") + "%";

		ИначеЕсли Данные.status = "processing" Тогда

			СтатусOCR = "processing";
			СтатусOCRПредставление = "Извлечение текста выполняется...";

			Если OCRПрогресс < 90 Тогда
				OCRПрогресс = OCRПрогресс + 5;
			КонецЕсли;

			OCRПрогрессТекст = "Извлечение текста выполняется... "
				+ Формат(OCRПрогресс, "ЧГ=0") + "%";

		ИначеЕсли Данные.status = "done" Тогда

			ОтключитьОбработчикОжидания("ПроверитьСтатусOCR");

			OCRПрогресс = 100;
			OCRПрогрессТекст = "Извлечение текста завершено";

			OCRВыполняется = Ложь;
			ОбновитьВидимостьOCRПрогресса();

			СтатусOCR = "ok";
			СтатусOCRПредставление = "Текст извлечён";

			Если Данные.Свойство("result") Тогда

				РезультатЗадачи = Данные.result;

				Если РезультатЗадачи.Свойство("document_id") Тогда
					ИДДокумента = РезультатЗадачи.document_id;
				КонецЕсли;

				Если РезультатЗадачи.Свойство("document_type") Тогда
					ТипДокумента = РезультатЗадачи.document_type;
					ТипДокументаПредставление = ПредставлениеТипаДокумента(ТипДокумента);
				КонецЕсли;

				Если РезультатЗадачи.Свойство("text_length") Тогда
					ДлинаOCRТекста = РезультатЗадачи.text_length;
				КонецЕсли;

				Если РезультатЗадачи.Свойство("ocr_from_cache") Тогда
					OCRИзКэша = РезультатЗадачи.ocr_from_cache;
				КонецЕсли;

				Если РезультатЗадачи.Свойство("ocr_used") Тогда
					OCRИспользовался = РезультатЗадачи.ocr_used;
				КонецЕсли;

				Если РезультатЗадачи.Свойство("ocr_result_dir") Тогда
					OCRКаталогРезультата = РезультатЗадачи.ocr_result_dir;
				КонецЕсли;

				Если РезультатЗадачи.Свойство("text_source") Тогда

					Если РезультатЗадачи.text_source = "pdf_text_layer" Тогда
						OCRИсточник = "Текстовый слой PDF";
					ИначеЕсли РезультатЗадачи.text_source = "cache" Тогда
						OCRИсточник = "Сохранённый результат";
					ИначеЕсли РезультатЗадачи.text_source = "ocr" Тогда
						OCRИсточник = "OCR";
					Иначе
						OCRИсточник = РезультатЗадачи.text_source;
					КонецЕсли;

				Иначе
					Если OCRИзКэша Тогда
						OCRИсточник = "Сохранённый результат";
					ИначеЕсли OCRИспользовался Тогда
						OCRИсточник = "OCR";
					Иначе
						OCRИсточник = "Текстовый слой PDF";
					КонецЕсли;
				КонецЕсли;

				Если РезультатЗадачи.Свойство("status") Тогда
					СтатусДокумента = РезультатЗадачи.status;
					СтатусДокументаПредставление = ПредставлениеСтатусаДокумента(СтатусДокумента);
				Иначе
					СтатусДокумента = "processed";
					СтатусДокументаПредставление = ПредставлениеСтатусаДокумента(СтатусДокумента);
				КонецЕсли;

				Если OCRИзКэша Тогда
					СтатусOCRПредставление = "Использован сохранённый текст";
				ИначеЕсли Не OCRИспользовался Тогда
					СтатусOCRПредставление = "Текст извлечён из PDF без OCR";
				Иначе
					СтатусOCRПредставление = "OCR выполнен";
				КонецЕсли;

			КонецЕсли;

			OCRTaskID = "";

			Сообщить(СтатусOCRПредставление);

		ИначеЕсли Данные.status = "failed" Тогда

			ОтключитьОбработчикОжидания("ПроверитьСтатусOCR");

			OCRВыполняется = Ложь;
			ОбновитьВидимостьOCRПрогресса();

			OCRПрогресс = 0;
			OCRПрогрессТекст = "Ошибка извлечения текста";

			СтатусOCR = "failed";
			СтатусOCRПредставление = "Ошибка извлечения текста";

			СтатусДокумента = "failed";
			СтатусДокументаПредставление = ПредставлениеСтатусаДокумента(СтатусДокумента);

			Если Данные.Свойство("error") Тогда
				Сообщить("Ошибка извлечения текста: " + Данные.error);
			Иначе
				Сообщить("Ошибка извлечения текста.");
			КонецЕсли;

			OCRTaskID = "";

		Иначе

			СтатусOCR = Данные.status;
			СтатусOCRПредставление = "Статус задачи: " + Данные.status;

			OCRПрогрессТекст = СтатусOCRПредставление;

		КонецЕсли;

	Исключение

		ОтключитьОбработчикОжидания("ПроверитьСтатусOCR");

		OCRВыполняется = Ложь;
		ОбновитьВидимостьOCRПрогресса();

		OCRПрогресс = 0;
		OCRПрогрессТекст = "Ошибка проверки статуса извлечения текста";

		СтатусOCR = "failed";
		СтатусOCRПредставление = "Ошибка проверки статуса извлечения текста";

		Сообщить("Ошибка проверки статуса OCR: " + ОписаниеОшибки());

		OCRTaskID = "";

	КонецПопытки;

КонецПроцедуры 

&НаКлиенте
Процедура ОбновитьВидимостьOCRПрогресса()

	Элементы.Группа9.Видимость = OCRВыполняется;

КонецПроцедуры



&НаКлиенте
Процедура ПоказатьТекстНажатие(Команда)

	Если ИДДокумента = 0 Тогда
		Сообщить("Сначала загрузите документ.");
		Возврат;
	КонецЕсли;

	Попытка

		Результат = ПолучитьOCRРезультатНаСервере(ИДДокумента);

		ОтветСервера = Результат;

		Данные = ПрочитатьJSONИзСтроки(Результат);

		Если Данные.Свойство("text") Тогда
			OCRТекст = Данные.text;
			HTMLТекстOCR = СформироватьHTMLДляOCRТекста(OCRТекст);
		КонецЕсли;    
		
		Если Данные.Свойство("text_length") Тогда
			ДлинаOCRТекста = Данные.text_length;
		КонецЕсли;

		Если Данные.Свойство("ocr_result_dir") Тогда
			OCRКаталогРезультата = Данные.ocr_result_dir;
		КонецЕсли;

		СтатусOCR = "Текст получен";

	Исключение
		Сообщить(
			"Не удалось получить OCR-текст. Возможно, документ ещё не распознавался."
			+ Символы.ПС
			+ ОписаниеОшибки()
		);
	КонецПопытки;

КонецПроцедуры


&НаКлиенте
Функция ПредставлениеСтатусаOCR(Статус)

	Если Статус = "ok" Тогда
		Возврат "Распознавание выполнено";
	ИначеЕсли Статус = "failed" Тогда
		Возврат "Ошибка распознавания";
	ИначеЕсли Статус = "Выполняется..." Тогда
		Возврат "Выполняется...";
	ИначеЕсли Статус = "Повторное распознавание..." Тогда
		Возврат "Повторное распознавание...";
	ИначеЕсли ПустаяСтрока(Статус) Тогда
		Возврат "Не выполнено";
	КонецЕсли;

	Возврат Статус;

КонецФункции

&НаКлиенте
Процедура РаспознатьЗановоНажатие(Команда)

	Если ИДДокумента = 0 Тогда
		Сообщить("Сначала загрузите документ.");
		Возврат;
	КонецЕсли;

	Попытка

		СтатусOCR = "processing";
		СтатусOCRПредставление = "Повторное распознавание запущено...";
		OCRИсточник = "";
		OCRИзКэша = Ложь;

		OCRТекст = "";
		HTMLТекстOCR = "";

		Результат = ЗапуститьOCRНаСервере(ИДДокумента, Истина);

		ОтветСервера = Результат;

		Данные = ПрочитатьJSONИзСтроки(Результат);

		Если Данные.Свойство("task_id") Тогда
			OCRTaskID = Данные.task_id;  
			OCRПрогресс = 5;
OCRПрогрессТекст = "Повторное распознавание запущено... 5%";
		Иначе
			Сообщить("OCR запущен, но сервер не вернул task_id.");
			Возврат;
		КонецЕсли;

		ПодключитьОбработчикОжидания("ПроверитьСтатусOCR", 5, Ложь);

		Сообщить("Повторное распознавание запущено.");

	Исключение

		СтатусOCR = "failed";
		СтатусOCRПредставление = "Ошибка запуска повторного OCR";

		Сообщить("Ошибка запуска повторного OCR: " + ОписаниеОшибки());

	КонецПопытки;

КонецПроцедуры

&НаКлиенте
Функция СформироватьHTMLДляOCRТекста(ТекстOCR)

	ОтформатированныйТекст = ПодготовитьOCRТекстДляHTML(ТекстOCR);

HTML = "";
	HTML = HTML + "<!DOCTYPE html>";
	HTML = HTML + "<html><head><meta charset='UTF-8'></head><body>";
	HTML = HTML + "<pre style='white-space: pre-wrap; font-family: Arial; font-size: 14px;'>";
	HTML = HTML + ЭкранироватьHTML(ТекстOCR);
	HTML = HTML + "</pre>";
	HTML = HTML + "</body></html>";

	Возврат HTML;;

	Возврат HTML;

КонецФункции

&НаКлиенте
Функция ЭкранироватьHTML(Текст)

	Если Текст = Неопределено Тогда
		Возврат "";
	КонецЕсли;

	Результат = Строка(Текст);

	Результат = СтрЗаменить(Результат, "&", "&amp;");
	Результат = СтрЗаменить(Результат, "<", "&lt;");
	Результат = СтрЗаменить(Результат, ">", "&gt;");
	Результат = СтрЗаменить(Результат, """", "&quot;");
	Результат = СтрЗаменить(Результат, "'", "&#39;");

	Возврат Результат;

КонецФункции


&НаКлиенте
Функция ПодготовитьOCRТекстДляHTML(ТекстOCR)

	Если ТекстOCR = Неопределено Тогда
		Возврат "";
	КонецЕсли;

	Текст = Строка(ТекстOCR);

	// Нормализуем переносы строк
	Текст = СтрЗаменить(Текст, Символы.ВК + Символы.ПС, Символы.ПС);
	Текст = СтрЗаменить(Текст, Символы.ВК, Символы.ПС);

	Строки = СтрРазделить(Текст, Символы.ПС);

	HTML = "";
	БлокОткрыт = Ложь;

	Для Каждого СтрокаТекста Из Строки Цикл

		СтрокаБезПробелов = СокрЛП(СтрокаТекста);

		ПозицияМаркера = СтрНайти(СтрокаБезПробелов, "--- Страница ");

		Если ПозицияМаркера = 1 Тогда

			Если БлокОткрыт Тогда
				HTML = HTML + "</div>";
				БлокОткрыт = Ложь;
			КонецЕсли;

			// Убираем начало маркера: "--- Страница "
			ПослеСловаСтраница = Сред(СтрокаБезПробелов, СтрДлина("--- Страница ") + 1);

			// Теперь ищем закрывающие дефисы уже в оставшейся строке
			ПозицияКонца = СтрНайти(ПослеСловаСтраница, " ---");

			Если ПозицияКонца > 0 Тогда

				НомерСтраницы = Лев(ПослеСловаСтраница, ПозицияКонца - 1);

				ОстатокСтроки = СокрЛП(Сред(
					ПослеСловаСтраница,
					ПозицияКонца + СтрДлина(" ---")
				));

			Иначе

				НомерСтраницы = ПослеСловаСтраница;
				ОстатокСтроки = "";

			КонецЕсли;

			HTML = HTML + "<br><div class='page-title'>Страница "
				+ ЭкранироватьHTML(НомерСтраницы)
				+ "</div><br>";

			HTML = HTML + "<div class='ocr-block'><br>";
			БлокОткрыт = Истина;

			Если Не ПустаяСтрока(ОстатокСтроки) Тогда
				HTML = HTML + ЭкранироватьHTML(ОстатокСтроки) + Символы.ПС;
			КонецЕсли;

		Иначе

			Если Не БлокОткрыт Тогда
				HTML = HTML + "<div class='ocr-block'>";
				БлокОткрыт = Истина;
			КонецЕсли;

			HTML = HTML + ЭкранироватьHTML(СтрокаТекста) + Символы.ПС;

		КонецЕсли;

	КонецЦикла;

	Если БлокОткрыт Тогда
		HTML = HTML + "</div><br>";
	КонецЕсли;

	Возврат HTML;

КонецФункции

&НаСервере
Функция ПолучитьPreviewСтраницНаСервере(ИДДокумента)

	Возврат ПолучитьPreviewСтраницДокумента(ИДДокумента);

КонецФункции

 &НаКлиенте
Функция СформироватьHTMLДляPreviewPDF(Страницы)

	HTML = "";
	HTML = HTML + "<!DOCTYPE html>";
	HTML = HTML + "<html>";
	HTML = HTML + "<head>";
	HTML = HTML + "<meta charset='UTF-8'>";
	HTML = HTML + "<style>";
	HTML = HTML + "body { margin:0; padding:16px; background:#f3f4f6; font-family:Arial, sans-serif; }";
	HTML = HTML + ".page { margin:0 auto 24px auto; max-width:900px; background:#fff; padding:12px; border:1px solid #ddd; box-shadow:0 2px 8px rgba(0,0,0,0.12); }";
	HTML = HTML + ".title { font-weight:bold; margin-bottom:10px; color:#333; }";
	HTML = HTML + "img { width:100%; height:auto; display:block; }";
	HTML = HTML + "</style>";
	HTML = HTML + "</head>";
	HTML = HTML + "<body>";

	Для Каждого Страница Из Страницы Цикл

		HTML = HTML + "<div class='page'>";
		HTML = HTML + "<div class='title'>Страница " + Строка(Страница.page) + "</div>";
		HTML = HTML + "<img src='" + Страница.image_url + "'>";
		HTML = HTML + "</div>";

	КонецЦикла;

	HTML = HTML + "</body>";
	HTML = HTML + "</html>";

	Возврат HTML;

КонецФункции

&НаКлиенте
Процедура ПоказатьСтраницыPDFНажатие(Команда)

	Если ИДДокумента = 0 Тогда
		Сообщить("Сначала загрузите документ.");
		Возврат;
	КонецЕсли;

	Попытка

		Результат = ПолучитьPreviewСтраницНаСервере(ИДДокумента);

		ОтветСервера = Результат;

		Данные = ПрочитатьJSONИзСтроки(Результат);

		Если Данные.Свойство("pages") Тогда
			HTMLПросмотрPDF = СформироватьHTMLДляPreviewPDF(Данные.pages);
			Сообщить("Preview страниц сформирован.");
		Иначе
			Сообщить("В ответе сервера нет списка страниц.");
		КонецЕсли;

	Исключение
		Сообщить("Ошибка отображения preview PDF: " + ОписаниеОшибки());
	КонецПопытки;

КонецПроцедуры



&НаКлиенте
Функция ПредставлениеТипаДокумента(ТипДокумента)

	Если ТипДокумента = "rent_contract" Тогда
		Возврат "Договор аренды";
	ИначеЕсли ТипДокумента = "auction_protocol" Тогда
		Возврат "Протокол торгов";
	ИначеЕсли ТипДокумента = "egrn" Тогда
		Возврат "Выписка ЕГРН";
	ИначеЕсли ТипДокумента = "sale_contract" Тогда
		Возврат "Договор купли-продажи";
	ИначеЕсли ТипДокумента = "unknown" Тогда
		Возврат "Тип не определён";
	ИначеЕсли ПустаяСтрока(ТипДокумента) Тогда
		Возврат "Не указан";
	КонецЕсли;

	Возврат ТипДокумента;

КонецФункции


&НаКлиенте
Функция ПредставлениеСтатусаДокумента(Статус)

	Если Статус = "uploaded" Тогда
		Возврат "Загружен";
	ИначеЕсли Статус = "processed" Тогда
		Возврат "Обработан";
	ИначеЕсли Статус = "failed" Тогда
		Возврат "Ошибка обработки";
	ИначеЕсли Статус = "ok" Тогда
		Возврат "Выполнено";
	ИначеЕсли Статус = "failed" Тогда
		Возврат "Ошибка";
	ИначеЕсли ПустаяСтрока(Статус) Тогда
		Возврат "Не выполнено";
	КонецЕсли;

	Возврат Статус;

КонецФункции


&НаСервере
Функция ВыполнитьAIАнализНаСервере(ИДДокумента)

	Возврат ВыполнитьAIАнализДокумента(ИДДокумента);

КонецФункции


&НаСервере
Функция ПолучитьAIРезультатНаСервере(ИДДокумента)

	Возврат ПолучитьAIРезультатДокумента(ИДДокумента);

КонецФункции

   &НаКлиенте
Функция ПредставлениеСтатусаAI(Статус)

	Если Статус = "ok" Тогда
		Возврат "AI-анализ выполнен";
	ИначеЕсли Статус = "failed" Тогда
		Возврат "Ошибка AI-анализа";
	ИначеЕсли Статус = "processing" Тогда
		Возврат "Выполняется AI-анализ";
	ИначеЕсли Статус = "not_found" Тогда
		Возврат "Результат анализа не найден";
	ИначеЕсли ПустаяСтрока(Статус) Тогда
		Возврат "Не выполнено";
	КонецЕсли;

	Возврат Статус;

КонецФункции  




&НаКлиенте
Процедура ПоказатьAIРезультатНажатие(Команда)

	Если ИДДокумента = 0 Тогда
		Сообщить("Сначала загрузите документ.");
		Возврат;
	КонецЕсли;

	Попытка

		Результат = ПолучитьAIРезультатНаСервере(ИДДокумента);

		ОтветСервера = Результат;

		Данные = ПрочитатьJSONИзСтроки(Результат);

		ЗаполнитьПоляAIИзОтвета(Данные);

		Сообщить("Результат AI-анализа получен.");

	Исключение

		Сообщить(
			"Не удалось получить результат AI-анализа. Возможно, анализ ещё не выполнялся."
			+ Символы.ПС
			+ ОписаниеОшибки()
		);

	КонецПопытки;

КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьПоляAIИзОтвета(Данные)

	Если Данные.Свойство("status") Тогда
		СтатусAI = Данные.status;
		СтатусAIПредставление = ПредставлениеСтатусаAI(СтатусAI);
	Иначе
		СтатусAI = "ok";
		СтатусAIПредставление = ПредставлениеСтатусаAI(СтатусAI);
	КонецЕсли;

	Если Данные.Свойство("document_type") Тогда
		ТипДокумента = Данные.document_type;
		ТипДокументаПредставление = ПредставлениеТипаДокумента(ТипДокумента);
	КонецЕсли;

	Если Данные.Свойство("resolved_data") Тогда
		AIРезультат = ЗаписатьJSONВСтроку(Данные.resolved_data);
		HTMLAIРезультат = СформироватьHTMLДляAIРезультата(Данные.resolved_data);
	ИначеЕсли Данные.Свойство("analysis_result") Тогда
		AIРезультат = ЗаписатьJSONВСтроку(Данные.analysis_result);
		HTMLAIРезультат = СформироватьHTMLДляAIРезультата(Данные.analysis_result);
	ИначеЕсли Данные.Свойство("result") Тогда
		AIРезультат = ЗаписатьJSONВСтроку(Данные.result);
		HTMLAIРезультат = СформироватьHTMLДляAIРезультата(Данные.result);
	Иначе
		AIРезультат = ЗаписатьJSONВСтроку(Данные);
		HTMLAIРезультат = СформироватьHTMLДляAIРезультата(Данные);
	КонецЕсли;

КонецПроцедуры


 

&НаСервере
Функция ЗапуститьOCRНаСервере(ИДДокумента, ForceOCR)

	Возврат ЗапуститьOCRДокумента(ИДДокумента, ForceOCR);

КонецФункции

&НаСервере
Функция ПолучитьСтатусЗадачиНаСервере(ИДЗадачи)

	Возврат ПолучитьСтатусЗадачи(ИДЗадачи);

КонецФункции


&НаСервере
Функция ПолучитьСтатусOCRНаСервере(ИДДокумента)

	Возврат ПолучитьСтатусOCRДокумента(ИДДокумента);

КонецФункции


&НаСервере
Функция ЗапуститьAIАнализНаСервере(ИДДокумента)

	Возврат ЗапуститьAIАнализДокумента(ИДДокумента);

КонецФункции

&НаКлиенте
Процедура ОбновитьВидимостьAIПрогресса()

	Элементы.ГруппаAIПрогресс.Видимость = AIВыполняется;

КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьAIАнализНажатие(Команда)

	Если ИДДокумента = 0 Тогда
		Сообщить("Сначала загрузите документ.");
		Возврат;
	КонецЕсли;

	Попытка

		СтатусAI = "processing";
		СтатусAIПредставление = "AI-анализ запускается...";

		AIВыполняется = Истина;
		AIПрогресс = 0;
		AIПрогрессТекст = "Запуск AI-анализа...";
		ОбновитьВидимостьAIПрогресса();

		AIРезультат = "";
		HTMLAIРезультат = "";

		Результат = ЗапуститьAIАнализНаСервере(ИДДокумента);

		ОтветСервера = Результат;

		Данные = ПрочитатьJSONИзСтроки(Результат);

		Если Данные.Свойство("task_id") Тогда

			AITaskID = Данные.task_id;

			AIПрогресс = 5;
			AIПрогрессТекст = "AI-анализ запущен... 5%";

		Иначе

			AIВыполняется = Ложь;
			ОбновитьВидимостьAIПрогресса();

			AIПрогресс = 0;
			AIПрогрессТекст = "Не удалось получить идентификатор задачи";

			Сообщить("AI-анализ запущен, но сервер не вернул task_id.");
			Возврат;

		КонецЕсли;

		// Важно: третий параметр Ложь, чтобы проверка повторялась
		ПодключитьОбработчикОжидания("ПроверитьСтатусAI", 5, Ложь);

		Сообщить("AI-анализ запущен.");

	Исключение

		AIВыполняется = Ложь;
		ОбновитьВидимостьAIПрогресса();

		СтатусAI = "failed";
		СтатусAIПредставление = "Ошибка запуска AI-анализа";

		AIПрогресс = 0;
		AIПрогрессТекст = "Ошибка запуска AI-анализа";

		Сообщить("Ошибка запуска AI-анализа: " + ОписаниеОшибки());

	КонецПопытки;

КонецПроцедуры

&НаКлиенте
Процедура ПроверитьСтатусAI()

	Если ПустаяСтрока(AITaskID) Тогда
		ОтключитьОбработчикОжидания("ПроверитьСтатусAI");
		Возврат;
	КонецЕсли;

	Попытка

		Результат = ПолучитьСтатусЗадачиНаСервере(AITaskID);

		Данные = ПрочитатьJSONИзСтроки(Результат);

		Если Данные.Свойство("status") Тогда

			Если Данные.status = "pending" Тогда

				СтатусAI = "pending";
				СтатусAIПредставление = "AI-анализ ожидает запуска...";

				Если AIПрогресс < 15 Тогда
					AIПрогресс = AIПрогресс + 2;
				КонецЕсли;

				AIПрогрессТекст = "Ожидание запуска AI-анализа... "
					+ Формат(AIПрогресс, "ЧГ=0") + "%";

			ИначеЕсли Данные.status = "processing" Тогда

				СтатусAI = "processing";
				СтатусAIПредставление = "AI-анализ выполняется...";

				Если AIПрогресс < 90 Тогда
					AIПрогресс = AIПрогресс + 5;
				КонецЕсли;

				AIПрогрессТекст = "AI-анализ выполняется... "
					+ Формат(AIПрогресс, "ЧГ=0") + "%";

			ИначеЕсли Данные.status = "done" Тогда

				ОтключитьОбработчикОжидания("ПроверитьСтатусAI");

				AIПрогресс = 100;
				AIПрогрессТекст = "AI-анализ завершён";

				AIВыполняется = Ложь;
				ОбновитьВидимостьAIПрогресса();

				СтатусAI = "ok";
				СтатусAIПредставление = "AI-анализ выполнен";

				Если Данные.Свойство("result") Тогда

					РезультатЗадачи = Данные.result;

					Если РезультатЗадачи.Свойство("document_type") Тогда
						ТипДокумента = РезультатЗадачи.document_type;
						ТипДокументаПредставление = ПредставлениеТипаДокумента(ТипДокумента);
					КонецЕсли;

					Если РезультатЗадачи.Свойство("resolved_data") Тогда
						AIРезультат = ЗаписатьJSONВСтроку(РезультатЗадачи.resolved_data);
						HTMLAIРезультат = СформироватьHTMLДляAIРезультата(РезультатЗадачи.resolved_data);
					КонецЕсли;

				КонецЕсли;

				AITaskID = "";

				Сообщить("AI-анализ завершён.");

			ИначеЕсли Данные.status = "failed" Тогда

				ОтключитьОбработчикОжидания("ПроверитьСтатусAI");

				AIВыполняется = Ложь;
				ОбновитьВидимостьAIПрогресса();

				AIПрогресс = 0;
				AIПрогрессТекст = "Ошибка AI-анализа";

				СтатусAI = "failed";
				СтатусAIПредставление = "Ошибка AI-анализа";

				Если Данные.Свойство("error") Тогда
					Сообщить("Ошибка AI-анализа: " + Данные.error);
				Иначе
					Сообщить("Ошибка AI-анализа.");
				КонецЕсли;

				AITaskID = "";

			КонецЕсли;

		КонецЕсли;

	Исключение

		ОтключитьОбработчикОжидания("ПроверитьСтатусAI");

		AIВыполняется = Ложь;
		ОбновитьВидимостьAIПрогресса();

		СтатусAI = "failed";
		СтатусAIПредставление = "Ошибка проверки AI-анализа";

		AIПрогресс = 0;
		AIПрогрессТекст = "Ошибка проверки AI-анализа";

		Сообщить("Ошибка проверки статуса AI-анализа: " + ОписаниеОшибки());

	КонецПопытки;

КонецПроцедуры


&НаКлиенте
Функция СформироватьHTMLДляAIРезультата(ДанныеAI)

	HTML = "";
	HTML = HTML + "<!DOCTYPE html>";
	HTML = HTML + "<html>";
	HTML = HTML + "<head>";
	HTML = HTML + "<meta charset='UTF-8'>";
	HTML = HTML + "<style>";
	HTML = HTML + "body { font-family: Arial, sans-serif; font-size: 14px; padding: 16px; background: #f3f4f6; color: #222; }";
	HTML = HTML + ".card { background: #fff; border: 1px solid #ddd; border-radius: 8px; padding: 16px; margin-bottom: 14px; box-shadow: 0 1px 4px rgba(0,0,0,0.08); }";
	HTML = HTML + ".title { font-size: 18px; font-weight: bold; margin-bottom: 12px; color: #1f2937; }";
	HTML = HTML + ".section-title { font-size: 15px; font-weight: bold; margin-bottom: 10px; color: #374151; }";
	HTML = HTML + ".row { display: flex; margin-bottom: 8px; border-bottom: 1px solid #f0f0f0; padding-bottom: 6px; }";
	HTML = HTML + ".label { width: 190px; color: #6b7280; font-weight: bold; flex-shrink: 0; }";
	HTML = HTML + ".value { color: #111827; flex: 1; white-space: pre-wrap; }";
	HTML = HTML + ".empty { color: #9ca3af; font-style: italic; }";
	HTML = HTML + ".two-columns { display: flex; gap: 14px; }";
	HTML = HTML + ".column { flex: 1; }";
	HTML = HTML + "</style>";
	HTML = HTML + "</head>";
	HTML = HTML + "<body>";

	HTML = HTML + "<div class='card'>";
	HTML = HTML + "<div class='title'>Извлечённые данные документа</div>";
	HTML = HTML + СтрокаПоляHTML("Номер договора", ПолучитьЗначениеAI(ДанныеAI, "contract_number"));
	HTML = HTML + СтрокаПоляHTML("Дата договора", ФорматироватьДатуAI(ПолучитьЗначениеAI(ДанныеAI, "contract_date")));
	HTML = HTML + "</div>";

	Если ДанныеAI.Свойство("parties") Тогда
		Стороны = ДанныеAI.parties;

		HTML = HTML + "<div class='card'>";
		HTML = HTML + "<div class='section-title'>Стороны договора</div>";
		HTML = HTML + СтрокаПоляHTML("Арендодатель", ПолучитьЗначениеAI(Стороны, "lessor_name"));
		HTML = HTML + СтрокаПоляHTML("Арендатор", ПолучитьЗначениеAI(Стороны, "lessee_name"));
		HTML = HTML + "</div>";
	КонецЕсли;

	Если ДанныеAI.Свойство("subject") Тогда
		Предмет = ДанныеAI.subject;

		HTML = HTML + "<div class='card'>";
		HTML = HTML + "<div class='section-title'>Предмет договора</div>";
		HTML = HTML + СтрокаПоляHTML("Кадастровый номер", ПолучитьЗначениеAI(Предмет, "cadastral_number"));
		HTML = HTML + СтрокаПоляHTML("Площадь", ПолучитьЗначениеAI(Предмет, "area"));
		HTML = HTML + СтрокаПоляHTML("Описание участка", ПолучитьЗначениеAI(Предмет, "subject_text"));
		HTML = HTML + "</div>";
	КонецЕсли;

	Если ДанныеAI.Свойство("term") Тогда
		Срок = ДанныеAI.term;

		HTML = HTML + "<div class='card'>";
		HTML = HTML + "<div class='section-title'>Срок аренды</div>";
		HTML = HTML + СтрокаПоляHTML("Дата начала", ФорматироватьДатуAI(ПолучитьЗначениеAI(Срок, "start_date")));
		HTML = HTML + СтрокаПоляHTML("Дата окончания", ФорматироватьДатуAI(ПолучитьЗначениеAI(Срок, "end_date")));
		HTML = HTML + "</div>";
	КонецЕсли;

	Если ДанныеAI.Свойство("rent_payment") Тогда
		Платеж = ДанныеAI.rent_payment;

		HTML = HTML + "<div class='card'>";
		HTML = HTML + "<div class='section-title'>Арендная плата</div>";
		HTML = HTML + СтрокаПоляHTML("Размер платежа", ПолучитьЗначениеAI(Платеж, "rent_amount"));
		HTML = HTML + СтрокаПоляHTML("Периодичность", ПолучитьЗначениеAI(Платеж, "payment_period"));
		HTML = HTML + "</div>";
	КонецЕсли;

	Если ДанныеAI.Свойство("party_details") Тогда
		Реквизиты = ДанныеAI.party_details;

		HTML = HTML + "<div class='card'>";
		HTML = HTML + "<div class='section-title'>Реквизиты сторон</div>";
		HTML = HTML + "<div class='two-columns'>";

		Если Реквизиты.Свойство("lessor") Тогда
			HTML = HTML + "<div class='column'>";
			HTML = HTML + "<div class='section-title'>Арендодатель</div>";
			HTML = HTML + СформироватьHTMLРеквизитовСтороны(Реквизиты.lessor);
			HTML = HTML + "</div>";
		КонецЕсли;

		Если Реквизиты.Свойство("lessee") Тогда
			HTML = HTML + "<div class='column'>";
			HTML = HTML + "<div class='section-title'>Арендатор</div>";
			HTML = HTML + СформироватьHTMLРеквизитовСтороны(Реквизиты.lessee);
			HTML = HTML + "</div>";
		КонецЕсли;

		HTML = HTML + "</div>";
		HTML = HTML + "</div>";
	КонецЕсли;

	HTML = HTML + "</body>";
	HTML = HTML + "</html>";

	Возврат HTML;

КонецФункции

&НаКлиенте
Функция СтрокаПоляHTML(Название, Значение)

	Если Значение = Неопределено Или ПустаяСтрока(Строка(Значение)) Тогда
		ЗначениеHTML = "<span class='empty'>не найдено</span>";
	Иначе
		ЗначениеHTML = ЭкранироватьHTML(Строка(Значение));
	КонецЕсли;

	HTML = "";
	HTML = HTML + "<div class='row'>";
	HTML = HTML + "<div class='label'>" + ЭкранироватьHTML(Название) + "</div>";
	HTML = HTML + "<div class='value'>" + ЗначениеHTML + "</div>";
	HTML = HTML + "</div>";

	Возврат HTML;

КонецФункции

&НаКлиенте
Функция ПолучитьЗначениеAI(СтруктураДанных, ИмяПоля)

	Если СтруктураДанных = Неопределено Тогда
		Возврат "";
	КонецЕсли;

	Если СтруктураДанных.Свойство(ИмяПоля) Тогда
		Возврат СтруктураДанных[ИмяПоля];
	КонецЕсли;

	Возврат "";

КонецФункции


&НаКлиенте
Функция СформироватьHTMLРеквизитовСтороны(ДанныеСтороны)

	HTML = "";
	HTML = HTML + СтрокаПоляHTML("ИНН", ПолучитьЗначениеAI(ДанныеСтороны, "inn"));
	HTML = HTML + СтрокаПоляHTML("КПП", ПолучитьЗначениеAI(ДанныеСтороны, "kpp"));
	HTML = HTML + СтрокаПоляHTML("ОГРН", ПолучитьЗначениеAI(ДанныеСтороны, "ogrn"));
	HTML = HTML + СтрокаПоляHTML("Юридический адрес", ПолучитьЗначениеAI(ДанныеСтороны, "legal_address"));
	HTML = HTML + СтрокаПоляHTML("Телефон", ПолучитьЗначениеAI(ДанныеСтороны, "phone"));

	Возврат HTML;

КонецФункции


&НаКлиенте
Функция ФорматироватьДатуAI(Значение)

	Если Значение = Неопределено Или ПустаяСтрока(Строка(Значение)) Тогда
		Возврат "";
	КонецЕсли;

	Текст = Строка(Значение);

	// Формат YYYY-MM-DD
	Если СтрДлина(Текст) = 10 
		И Сред(Текст, 5, 1) = "-" 
		И Сред(Текст, 8, 1) = "-" Тогда

		Год = Лев(Текст, 4);
		Месяц = Сред(Текст, 6, 2);
		День = Прав(Текст, 2);

		Возврат День + "." + Месяц + "." + Год;

	КонецЕсли;

	Возврат Текст;

КонецФункции



&НаСервере
Функция ПолучитьСписокДокументовНаСервере()

	Возврат ПолучитьСписокДокументов();

КонецФункции





&НаКлиенте
Процедура ОбновитьСписокДокументовНажатие(Команда)

	Попытка

		Результат = ПолучитьСписокДокументовНаСервере();

		ОтветСервера = Результат;

		Данные = ПрочитатьJSONИзСтроки(Результат);

		ЗаполнитьСписокДокументов(Данные);

		Сообщить("Список документов обновлён.");

	Исключение
		Сообщить("Ошибка получения списка документов: " + ОписаниеОшибки());
	КонецПопытки;

КонецПроцедуры


&НаКлиенте
Процедура ЗаполнитьСписокДокументов(Данные)

	СписокДокументов.Очистить();

	// DRF может вернуть либо массив, либо структуру с results
	Если ТипЗнч(Данные) = Тип("Структура") И Данные.Свойство("results") Тогда
		Документы = Данные.results;
	Иначе
		Документы = Данные;
	КонецЕсли;

	Для Каждого Документ Из Документы Цикл

		НоваяСтрока = СписокДокументов.Добавить();

		Если Документ.Свойство("id") Тогда
			НоваяСтрока.ИДДокумента = Документ.id;
		КонецЕсли;

		Если Документ.Свойство("original_filename") Тогда
			НоваяСтрока.ИмяФайла = Документ.original_filename;
		ИначеЕсли Документ.Свойство("file") Тогда
			НоваяСтрока.ИмяФайла = Документ.file;
		Иначе
			НоваяСтрока.ИмяФайла = "";
		КонецЕсли;

		Если Документ.Свойство("document_type") Тогда
			НоваяСтрока.ТипДокумента = Документ.document_type;
			НоваяСтрока.ТипДокументаПредставление =
				ПредставлениеТипаДокумента(Документ.document_type);
		КонецЕсли;

		Если Документ.Свойство("status") Тогда
			НоваяСтрока.СтатусДокумента = Документ.status;
			НоваяСтрока.СтатусДокументаПредставление =
				ПредставлениеСтатусаДокумента(Документ.status);
		КонецЕсли;

		Если Документ.Свойство("ocr_used") Тогда
			НоваяСтрока.OCRИспользовался = Документ.ocr_used;
		КонецЕсли;

		Если Документ.Свойство("created_at") Тогда
			НоваяСтрока.ДатаЗагрузки = Документ.created_at;
		КонецЕсли;

	КонецЦикла;

КонецПроцедуры



&НаКлиенте
Процедура ВыбратьДокументИзСпискаНажатие(Команда)

	ТекущаяСтрока = Элементы.СписокДокументов.ТекущиеДанные;

	Если ТекущаяСтрока = Неопределено Тогда
		Сообщить("Выберите документ в списке.");
		Возврат;
	КонецЕсли;

	ИДДокумента = ТекущаяСтрока.ИДДокумента;

	ИмяФайла = ТекущаяСтрока.ИмяФайла;

	ТипДокумента = ТекущаяСтрока.ТипДокумента;
	ТипДокументаПредставление = ТекущаяСтрока.ТипДокументаПредставление;

	СтатусДокумента = ТекущаяСтрока.СтатусДокумента;
	СтатусДокументаПредставление = ТекущаяСтрока.СтатусДокументаПредставление;

	OCRИспользовался = ТекущаяСтрока.OCRИспользовался;

	// Очищаем старые отображаемые результаты,
	// чтобы не показывать OCR/AI от предыдущего документа.
	OCRТекст = "";
	HTMLТекстOCR = "";

	AIРезультат = "";
	HTMLAIРезультат = "";

	HTMLПросмотрPDF = "";

	Сообщить("Выбран документ ID: " + Формат(ИДДокумента, "ЧГ=0"));

КонецПроцедуры


&НаКлиенте
Процедура ТаблицаСписокДокументовВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)

	СтандартнаяОбработка = Ложь;

	ВыбратьДокументИзСпискаНажатие(Неопределено);

КонецПроцедуры



